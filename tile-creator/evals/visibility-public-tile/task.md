# Create a Public Package Documentation Tile

I want to create an open-source tile that documents our logging library so anyone can find and use it. The tile should be "acme/logging-sdk", summary "Logging SDK for structured logs", placed at /tmp/logging-sdk-tile. It documents the npm package "logging-sdk" version 3.2.0. Include docs with an index.md and an api.md.
